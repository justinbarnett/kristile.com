.tabbable
    ul.nav.nav-tabs.nav-justified
        li(ng-repeat='tab in controller.tabs', ng-class='{active:tab.selected}')
          a(href='', ng-click='controller.select(tab)') {{tab.caption}}
      .tab-content(ng-transclude='ng-transclude')
